using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoroutineTest : MonoBehaviour
{
    

    void Update()
    {
        if (Input.GetKeyDown("space"))
        {
        }
       
    }


    //coroutine = fonction qui se lit à part, pas dans la fonction
    //utiliser dans la corout : foreach caravel yield return new WaitForSeconds
   



}
